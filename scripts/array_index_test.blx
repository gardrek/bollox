fun each(arr) {
    var i = -1;
    return fun() {
        i += 1;
        return arr[i];
    };
}

var ind = [ 0/0, 1/0, -1/0, 0, 0.01, 0.5, 0.9999, 1, 2, 3, 4, 5];
var a = ["a", "b", "c", "d", "e"];

for i in each(ind) {
    print to_string(i) + ": " + to_string(a[i]);
}
