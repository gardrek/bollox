var cycles = 10;
var time = 0;
while cycles > 0 {
    var start = clock();

    while (clock() < start + 1) {
        //print clock();
    }

    time = clock() / 1;

    print time;

    cycles = cycles - 1;
}